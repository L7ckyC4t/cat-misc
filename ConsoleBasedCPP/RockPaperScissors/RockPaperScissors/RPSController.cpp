#include "RPSController.h"

RPSController::RPSController(RPSModel* state, RPSView* view) : state(state), view(view)
{
}

void RPSController::startGame()
{
	/*
		welcome

		
		score (how many wins, how many losses, how many draws)

		display player vs computer (basically player: ? vs ? :computer)
		ask for input
		display player vs computer (basically player: rock vs rock: computer)


	*/
}
